syntax = "proto3";

package dynamo_db;

service Dynamo_DB {
  rpc CheckFile (Request) returns (Response) {}
  rpc UpdateFile (Request) returns (Response) {}
}

message Request {
  string file_hash = 2;
}

message Response {
  bool is_exist = 1;
}